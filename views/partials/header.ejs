<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title || 'Rummy App' %></title>
    <style>
      body { font-family: Arial, sans-serif; max-width: 860px; margin: 0 auto; padding: 2rem; }
      header nav a { margin-right: 1rem; }
      .error { color: #c00; margin: 1rem 0; }
      .box { padding: 1rem; border: 1px solid #ddd; border-radius: 8px; background: #fafafa; }
    </style>
  </head>
  <body>
    <header>
      <nav>
        <a href="/">Home</a>
        <% if (user) { %>
          <a href="/lobby">Lobby</a>
          <form style="display:inline" method="post" action="/auth/logout"><button type="submit">Logout</button></form>
        <% } else { %>
          <a href="/auth/login">Login</a>
          <a href="/auth/register">Register</a>
        <% } %>
      </nav>
    </header>
    <main>
